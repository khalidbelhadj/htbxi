[project]
name = "backend"
version = "0.1.0"
description = "Backend service for the gRPC server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "grpcio>=1.70.0",
    "grpcio-tools>=1.70.0",
]
